var ProfileListController = function () {    var removeUser = function () {        $('#modal_remove').on('show.bs.modal', function (event) {            var button = $(event.relatedTarget);            console.log(button.data("name"));            var modal = $(this);            modal.find("button.btn_remove_ok")                .data("id", button.data('id'));            modal.find(".modal-body").find(".name").html(button.data("name"));        });    }        var removeConfirm = function () {        $(".btn_remove_ok").click(function (e) {            e.preventDefault();            var id = $(this).data("id");            var href = Routing.generate('app_account_remove');            var data = {id: $.trim(id)};            // console.log($(this).data("id"));            // console.log(data);            if (href != null && data != null) {                $.getJSON(href, data).done(function (data) {                    // console.log(data);                    if (data.result == "success") {                        $('#line_user_' + id).fadeOut(500, function () {                            $(this).remove();                        });                        notification({                            icon: 'fa fa-check',                            title: "",                            message: data.message,                            type: "success",                        });                    } else {                        notification({                            icon: 'fa fa-warning',                            title: "",                            message: data.message,                            type: "danger",                        });                    }                });            }        });    }    var notification = function (option) {        $.notify({            icon: option.icon,            title: option.title,            message: option.message,        }, {            // settings            element: 'body',            position: null,            type: option.type,            allow_dismiss: true,            newest_on_top: false,            showProgressbar: false,            placement: {                from: "top",                align: "right"            },            offset: 55,            spacing: 10,            z_index: 9999,            delay: 2000,            timer: 500,            url_target: '_blank',            mouse_over: null,            animate: {                enter: 'animated fadeInDown',                exit: 'animated fadeOutUp'            },            onShow: null,            onShown: null,            onClose: null,            onClosed: null,            icon_type: 'class',            template: '<div data-notify="container" class="col-xs-11 col-sm-3 alert alert-{0}" role="alert">' +            '<button type="button" aria-hidden="true" class="close" data-notify="dismiss">×</button>' +            '<span data-notify="icon"></span> ' +            '<b><span data-notify="title">{1}</span></b> ' +            '<span data-notify="message">{2}</span>' +            '<div class="progress" data-notify="progressbar">' +            '<div class="progress-bar progress-bar-{0}" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" style="width: 0%;"></div>' +            '</div>' +            '</div>'        });    }    var btnSwitch = function () {        $('input[data-toggle="toggle"]').change(function () {            var state = $(this).prop('checked');            var id = $.trim($(this).attr('data-notify-id'));            var href = Routing.generate('app_account_state');            console.log(state);            if (id) {                $.getJSON(href, {"id": id, "enable": state})                    .done(function (data) {                        if (data.result == "success" && data.enable) {                            notification({                                icon: 'fa fa-check',                                title: "",                                message: data.message,                                type: "success",                            });                        } else if (data.result == "success" && !data.enable) {                            notification({                                icon: 'fa fa-warning',                                title: "",                                message: data.message,                                type: "warning",                            });                        } else {                            notification({                                icon: 'fa fa-warning',                                title: "",                                message: data.message,                                type: "danger",                            });                        }                    });            }        });        $(".td-enable")            .find('.loading').remove();        $(".td-enable").find(".btn-enable").removeClass('hide');    }    return {        inti: function () {            removeUser();            removeConfirm();            btnSwitch();        }    }}();$(function () {    ProfileListController.inti();})